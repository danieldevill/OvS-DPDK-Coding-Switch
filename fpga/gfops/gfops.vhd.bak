library ieee;
use ieee.std_logic_1164.all;
use ieee.numeric_std.all;

entity gfops is
generic(m : integer := 8);
port(
	 -- Inputs
	 clk : in std_logic;
	 rst : in std_logic;
	 operand_1 : in std_logic_vector(m-1 downto 0);
	 operand_2 : in std_logic_vector(m-1 downto 0);
	 sel : in std_logic;

	 -- Outputs
	 rslt : out std_logic_vector(m-1 downto 0));

end entity gfops;

architecture rtl of gfops is
type selection_type is (add, mul);

	signal selection : selection_type;

	process(clk,rst) is
	begin
		case (sel) is
			when '0' =>
				
			when others =>
				null;
		end case;

	end process;

begin

end architecture ; -- rtl